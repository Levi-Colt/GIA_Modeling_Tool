[pytest]
pythonpath = .
markers =
    cryocloud: test depends on real system RAM/resources and should be run inside a CryoCloud container to be meaningful, not just on any dev machine.
filterwarnings =
    always
